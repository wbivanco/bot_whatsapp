OPENAI_API_KEY = ''

### Embeddings ###
PERSIST_CHROMADB_FOLDER = 'db/dbchroma'

### File manager ###
# Carpeta local del servidor; no se versiona el contenido (ver .gitignore files/uploads/*).
PATH_TO_UPLOAD_FOLDER = 'files/uploads'
# Extensiones indexables (recomendado: separadas por coma, con punto).
# También acepta lista estilo Python, p. ej. ['.pdf', '.docx', '.txt'] — load_env la normaliza.
FILES_TYPES = .pdf,.docx,.txt

CHUNK_SIZE = 3000
CHUNK_OVERLAP = 40
# current_dir = os.path.dirname(__file__)
# upload_directory = os.path.join(current_dir, "../../uploads")

# Whatsapp
TOKEN_WHATSAPP = ''
API_URL = 'https://graph.facebook.com/v20.0/472290845963561/messages'

# Langsmith
LANGCHAIN_TRACING_V2=true
LANGCHAIN_ENDPOINT="https://api.smith.langchain.com"
LANGCHAIN_API_KEY=""
LANGCHAIN_PROJECT="bot_whatsapp"